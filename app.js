// Lógica simples de progresso (exemplo inicial)
document.addEventListener('DOMContentLoaded', () => {
  console.log('Hub do curso carregado.');
  // Aqui podemos adicionar a lógica de ler o LocalStorage e atualizar as barras de progresso
});
